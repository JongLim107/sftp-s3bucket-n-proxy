# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auth-svc`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auth-svc`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3012
EXPOSE 3012

WORKDIR /app

RUN addgroup --system auth-svc && \
  adduser --system -G auth-svc auth-svc

COPY auth-svc auth-svc/
RUN chown -R auth-svc:auth-svc .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auth-svc --omit=dev -f install

CMD [ "node", "auth-svc" ]
