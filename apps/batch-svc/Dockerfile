# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build batch-svc`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t batch-svc`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=8081
EXPOSE 8081

WORKDIR /app

RUN addgroup --system batch-svc && \
  adduser --system -G batch-svc batch-svc

COPY dist/apps/batch-svc batch-svc/
RUN chown -R batch-svc:batch-svc .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix batch-svc --omit=dev -f install

CMD [ "node", "batch-svc" ]
